{
  "name": "node-template",
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --coverage",
    "format": "prettier --write '**/*.{js,json,md}'"
  },
  "dependencies": {
    "jest": "^27.5.1",
    "prettier": "^2.6.2"
  },
  "wallaby": {
    "env": {
      "params": {
        "runner": "--experimental-vm-modules"
      }
    }
  }
}
